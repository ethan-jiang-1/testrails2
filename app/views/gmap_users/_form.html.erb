<%= form_for(@gmap_user) do |f| %>
  <% if @gmap_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gmap_user.errors.count, "error") %> prohibited this gmap_user from being saved:</h2>

      <ul>
      <% @gmap_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <p>
      <%= f.label :name %>
      <%= f.text_field :name %>
    </p>
    <p>
      <%= f.label :address %>
      <%= f.text_field :address %>
    </p>
    <p>
      <%= f.label :latitude %>
      <%= f.text_field :latitude %>
    </p>
    <p>
      <%= f.label :longitude %>
      <%= f.text_field :longitude %>
    </p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
