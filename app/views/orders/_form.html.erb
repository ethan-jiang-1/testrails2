<%= simple_form_for(@order, :html => {:class => 'form-horizontal'}) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :track_no %>
  <%= f.input :details %>
   <!--
  <%= f.hint 'No special characters in product name, please!' %>
  <%= f.error :product, :id => 'product_error' %>
  <%= f.full_error :token %>
-->
  <%= f.association :customer, :collection => Company.all(:order => 'name'), :prompt => "Choose a Company" %>

 <!-- %= f.input :order_date %-->

  <div class="control-group">
    <%= f.label :order_date %>
    <div class="controls">
      <input type="text" id="order_date" name="order[order_date]" data-behaviour='datepicker' value="2012-10-01">
      <!-- %= f.input_field :order_date, html: {databe-haviour: 'datepicker'} % -->
      <!-- <div class="input-append date" id="order_date" name="order[order_date]" data-date="12-02-2012" data-date-format="dd-mm-yyyy">
      <input class="span2" size="16" type="text" value="12-02-2012" data-behaviour='datepicker'>
      <span class="add-on"><i class="icon-th"></i></span>
    </div> -->

    </div>
  </div>


  <%= f.button :submit, :class => 'btn btn-inverse' %>
<script>
$(function(){
$("[data-behaviour~='datepicker']").datepicker({
    "format": "yyyy-mm-dd",
    "weekStart": 1,
    "autoclose": true
});
});
</script>

<!--
$(document).on("focus", "[data-behaviour~='datepicker']", function(e){
    $(this).datepicker({"format": "yyyy-mm-dd", "weekStart": 1, "autoclose": true});
});
-->
<% end %>
